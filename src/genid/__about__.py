"""The `__about__` module exposes the version of the `genid` package:

Example:

```python
from genid.__about__ import __version__
print(__version__)
```

Note that container version can also be imported directly from `genid` package:

Example:

```python
from genid import __version__
print(__version__)
```
"""
__version__ = "1.0.1"
